class Ingredient_JSON {
    constructor(name,origin, history, id, allergens) {
        this.name = name;
        this.origin = origin;
        this.history = history;
        this.id = id;
        this.allergens = allergens;
    }
}

module.exports = Ingredient_JSON;